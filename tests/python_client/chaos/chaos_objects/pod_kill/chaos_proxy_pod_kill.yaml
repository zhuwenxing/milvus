apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: test-proxy-pod-kill
  namespace: chaos-testing
spec:
  selector:
    namespaces:
      - chaos-testing
    labelSelectors:
      app.kubernetes.io/instance: s3-pod-kill-12941
      component: proxy
  mode: fixed
  value: "1"
  action: pod-kill
  duration: 2m
  gracePeriod: 0